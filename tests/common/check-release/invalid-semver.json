{
  "_comment": "Invalid semver tag — expect exit 1",
  "expected_exit": 1,
  "github_ref": "refs/tags/bad-ver",
  "inputs": { "version-format": "semver" },
  "event": { "release": { "target_commitish": "main" } }
}
