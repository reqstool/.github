{
  "_comment": "Valid tag targeting a release/* branch — expect exit 0",
  "expected_exit": 0,
  "github_ref": "refs/tags/1.2.3",
  "inputs": { "version-format": "semver" },
  "event": { "release": { "target_commitish": "release/1.2" } }
}
