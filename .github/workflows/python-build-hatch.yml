name: Build (Python / Hatch)

on:
  workflow_call:
    inputs:
      working-directory:
        description: "Project root relative to repo root. Default '.' for repo-root projects."
        required: false
        type: string
        default: "."
      python-version:
        description: "Python version to use."
        required: false
        type: string
        default: "3.13"
      coverage-source:
        description: "Package name passed to --cov=<name>. Empty = run tests without coverage."
        required: false
        type: string
        default: ""

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ${{ inputs.working-directory }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v6
        with:
          python-version: ${{ inputs.python-version }}

      - run: pip install hatch

      - name: Run tests (with coverage)
        if: ${{ inputs.coverage-source != '' }}
        run: |
          hatch run dev:pytest \
            --junitxml=build/junit.xml \
            --cov=${{ inputs.coverage-source }} \
            --cov-report=xml:build/coverage.xml

      - name: Run tests (without coverage)
        if: ${{ inputs.coverage-source == '' }}
        run: hatch run dev:pytest --junitxml=build/junit.xml

      - name: Build
        run: hatch build

      - uses: actions/upload-artifact@v6
        with:
          name: dist
          path: ${{ inputs.working-directory }}/dist/
